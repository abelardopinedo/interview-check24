nelmio_api_doc:
    documentation:
        info:
            title: CHECK24 Interview API
            description: API documentation for the CHECK24 interview task.
            version: 1.0.0
        components:
            securitySchemes:
                Bearer:
                    type: http
                    scheme: bearer
                    bearerFormat: JWT
    areas:
        path_patterns:
            - ^/calculate
            - ^/providers
            - ^/admin/logs
